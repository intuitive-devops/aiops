apiVersion: v1
kind: Service
metadata:
  name: aiops-level-one-service
  namespace: aiops
  labels:
    app: aiops-L1
spec:
  type: ClusterIP
  selector:
    app: aiops-L1
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080